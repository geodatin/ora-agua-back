version: "3.7"

services: 
    app:    
        build: 
          context: ./
          dockerfile: dockerfile.dev
        container_name: ora-agua
        ports: 
            - 3000:3000
        volumes: 
            - .:/usr/app